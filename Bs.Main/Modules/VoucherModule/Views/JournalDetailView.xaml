<Window x:Class="Bs.Main.Modules.VoucherModule.Views.JournalDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bs.Main.Modules.VoucherModule.Views" 
        xmlns:viewmodels="clr-namespace:Bs.Main.Modules.VoucherModule.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:JournalEntryDetailVm}"
        mc:Ignorable="d"
        FontSize="14"
        Title="JournalDetailView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="8"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Width" Value="150"/>
                <Setter Property="Margin" Value="3"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Width" Value="100"/>
                <Setter Property="Margin" Value="3"/>
                <Setter Property="Padding" Value="3"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Journal Account:"/>
        <ComboBox Grid.Row="1" Grid.Column="1" SelectedValue="{Binding JournalEntry.JournalAccountName}"/>
        
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Amount:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding JournalEntry.Amount}"/>
        <TextBlock Grid.Row="2" Grid.Column="2" Text="W.Tax Rate:"/>
        <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding JournalEntry.WithholdingTaxRate}"/>
        
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Net Amount:"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding JournalEntry.NetAmount}"/>
        
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Tax Amount:"/>
        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding JournalEntry.TaxAmount}"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Particulars:"/>
        <DataGrid Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="5" 
                  ItemsSource="{Binding JournalEntry.Particulars}">
        </DataGrid>


        <Button Grid.Row="7" Grid.Column="5" 
                HorizontalAlignment="Right" 
                Content="Save" IsDefault="True"
                Click="Button_Click"/>
    </Grid>
</Window>
